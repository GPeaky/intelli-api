max_connections = 200                  # Increase if you expect more concurrent connections
shared_buffers = 4GB                   # Increase to 25-50% of system RAM
work_mem = 64MB                        # Increase depending on your available RAM and query complexity
maintenance_work_mem = 1GB             # Increase for better performance during maintenance tasks
effective_cache_size = 4GB             # Set to approx 50% of total RAM
wal_buffers = 16MB                     # Increase WAL buffers
checkpoint_completion_target = 0.9     # Increase to spread out checkpoints for smoother I/O
